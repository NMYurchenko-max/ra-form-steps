// Этот файл содержит пользовательские команды Cypress
// Подробнее: https://on.cypress.io/custom-commands

// Пример пользовательской команды
// Cypress.Commands.add('login', (email, password) => { ... })

// Пример команды для получения элемента по data-атрибуту
// Cypress.Commands.add('getByData', (selector) => {
//   return cy.get(`[data-cy=${selector}]`)
// })
